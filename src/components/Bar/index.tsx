import styles from './styles.module.scss'

const Bar = () => {
    return (
        <section className={styles.bar}>
            <span>Novos artigos todos os sábados!</span>
        </section>
    )
}

export default Bar;